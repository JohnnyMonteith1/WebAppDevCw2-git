<!DOCTYPE html>
<html lang="en" style="height: 100%;">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Users</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        html, body {
            height: 100%;
            margin: 0;
        }
        body {
            display: flex;
            flex-direction: column;
        }
        .content {
            flex: 1;
        }
        .navbar-custom { background-color: #f8f9fa; }
        .navbar-custom .navbar-nav .nav-link { color: black; font-weight: bold; }
        .navbar-custom .navbar-nav .nav-link:hover { color: #505050; }
        .footer { 
            background-color: #333; 
            color: white; 
            text-align: center; 
            padding: 10px; 
        }
        ul {
            list-style-type: none;
            padding: 0;
        }
        li {
            margin-bottom: 20px;
        }
        form {
            display: inline;
        }
        button[type="submit"] {
            background-color: #dc3545;
            color: white;
            padding: 5px 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        button[type="submit"]:hover {
            background-color: #c82333;
        }
        a {
            margin-right: 10px;
            text-decoration: none;
            color: #007bff;
            font-weight: bold;
        }
        a:hover {
            color: #0056b3;
        }
        .navbar-brand img {
            max-height: 50px;
        }
    </style>
</head>
<body>
    <!-- Navigation bar using Bootstrap's navbar component -->
    <nav class="navbar navbar-expand-lg navbar-custom">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">
                <img src="/TSPN_logo.png" alt="TSPN Logo">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <li class="nav-item"><a class="nav-link" href="/adminHome">Home</a></li>
                    <li class="nav-item"><a class="nav-link" href="/adminAbout">About Us</a></li>
                    <li class="nav-item"><a class="nav-link" href="/adminContactUs">Contact Us</a></li>
                    <li class="nav-item"><a class="nav-link" href="/adminAddItems">Add Items</a></li>
                    <li class="nav-item"><a class="nav-link" href="/adminItems">Available Items</a></li>
                    <li class="nav-item"><a class="nav-link" href="/adminAddUser">Add Users</a></li>
                    <li class="nav-item"><a class="nav-link" href="/adminDeleteUser">Delete Users</a></li>
                    <li class="nav-item"><a class="nav-link" href="/logout">Sign Out</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Delete Users List -->
    <div class="container mt-3">
        <h1>Delete Users</h1>
        <ul>
            {{#users}}
            <li>
                <div>
                    <p>Username: {{username}}</p>
                    <p>Password: {{password}}</p>
                    <p>Role: {{role}}</p>
                    <form action="/adminDeleteUser/{{_id}}" method="post">
                        <input type="hidden" name="userId" value="{{_id}}">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </div>
            </li>
            {{/users}}
        </ul>
    </div>

    <!-- Footer -->
    <div class="footer">
        <p>&copy; 2024 The Scottish Pantry Network. All rights reserved.</p>
    </div>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
